#ifndef CONSTANTS_H
#define CONSTANTS_H

enum StrategyType
{
    SDL_STRATEGY,
    VRJUGGLER_STRATEGY
};

enum ReturnStatus
{
    RETURN_NORMAL,
    RETURN_EXIT
};

enum Direction
{
    LEFT,
    RIGHT,
    FRONT,
    BEHIND
};

#endif // CONSTANTS_H
